# Authentication — set the key(s) needed by your chosen agent:
ANTHROPIC_API_KEY=sk-ant-...          # for claude-code-acp
# CLAUDE_CODE_OAUTH_TOKEN=your-oauth-token
# OPENAI_API_KEY=sk-...               # for codex-acp

# Agent — single-agent mode (legacy):
ACP_AGENT_COMMAND=claude-code-acp     # or "codex-acp"
ENABLED_SERVICES=linear               # comma-separated: "linear", "slack", "github"

# Agent — multi-agent mode (overrides ACP_AGENT_COMMAND):
# Each key is an agent name; "default: true" marks the primary agent.
# If not set, falls back to ACP_AGENT_COMMAND for single-agent setup.
# AGENTS_JSON={"claude": {"command": "claude-code-acp", "default": true}, "codex": {"command": "codex-acp"}}

# Shared repo — all sessions work on this GitHub repo:
GITHUB_REPO=owner/repo               # e.g. "myorg/my-app"
GITHUB_INSTALLATION_ID=12345         # GitHub App installation ID (for non-webhook sessions)

# GitHub App — default agent (or single-agent mode)
GITHUB_APP_ID=
GITHUB_PRIVATE_KEY=                   # PEM key with \n for newlines
GITHUB_WEBHOOK_SECRET=
# GITHUB_BOT_LOGIN=my-app[bot]       # Optional fallback if auto-detect fails

# GitHub App — additional agents use __AGENTNAME suffix:
# GITHUB_APP_ID__CODEX=67890
# GITHUB_PRIVATE_KEY__CODEX=-----BEGIN RSA...
# GITHUB_WEBHOOK_SECRET__CODEX=whsec_codex
# GITHUB_BOT_LOGIN__CODEX=codex-bot[bot]

# Linear
LINEAR_WEBHOOK_SECRET=
LINEAR_ACCESS_TOKEN=
LINEAR_AGENT_ID=

# Slack — default agent (or single-agent mode)
SLACK_APP_TOKEN=xapp-1-...            # App-level token for Socket Mode
SLACK_BOT_TOKEN=xoxb-...              # Bot token for Web API
SLACK_USER_TOKEN=xoxp-...             # User token for search API

# Slack — additional agents use __AGENTNAME suffix:
# SLACK_BOT_TOKEN__CODEX=xoxb-codex-bot-token
# SLACK_APP_TOKEN__CODEX=xapp-codex-app-token
